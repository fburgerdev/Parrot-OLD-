#pragma once

namespace Parrot
{
	namespace Math
	{
		static constexpr float PI = 3.14159f;
		static constexpr float TAU = 2 * PI;
		static constexpr float PI_HALFS = PI / 2;
		static constexpr float PI_QUARTERS = PI / 4;

		static constexpr float EULER = 2.71828f; // lim (1 + 1 / n) ^ n

		static constexpr float GOLDEN_RATIO = 1.61803f; // 0.5 * (1 + sqrt(5))
	}
}