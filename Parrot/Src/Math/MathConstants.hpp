#pragma once

namespace Math
{
	static constexpr float PI = 3.14159f;
	static constexpr float TAU = 2 * PI;
	static constexpr float PI_HALFS = PI / 2;
	static constexpr float PI_QUARTERS = PI / 4;

	static constexpr float EULER = 2.71828f;

	static constexpr float GOLDEN_RATIO = 1.61803f;
}